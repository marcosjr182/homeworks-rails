<tr>
  <td><%= subject.name %></td>
  <td><%= link_to 'Show', url_for([@grade, subject]) %></td>
  <td><a href="/grades/<%= @grade.id %>/subjects/<%= subject.id %>/edit">Edit</a></td>
  <td><%= link_to 'Destroy', subject, method: :delete, data: { confirm: 'Are you sure?' } %></td>
</tr>