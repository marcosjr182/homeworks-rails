<tr>
  <td><%= subject.name %></td>
  <td><%= link_to 'Show', grade_subject_path(@grade, params[:id]) %></td>
  <td><%= link_to 'Edit', edit_grade_subject_path(:grade_id,:id) %></td>
  <td><%= link_to 'Destroy', grade_subject_path(:grade_id, :id), method: :delete, data: { confirm: 'Are you sure?' } %></td>
</tr>